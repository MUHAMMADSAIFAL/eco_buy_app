import 'package:flutter/material.dart';

class Dashbord extends StatelessWidget {
  static const String id = "Dashbord";

  const Dashbord({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Dashbord"),
      ),
    );
  }
}
