import 'dart:math';
import 'package:flutter/material.dart';

final customColor = Colors.primaries[Random().nextInt(Colors.primaries.length)];

class Appcolor {
  static const background = [Color(0xFFB3E5FC), Color(0xFF4A00E0)];
  static const Appbar = Color(0xFFFF416C);

  static const border = Color(0xFF4A00E0);
  static const red = Colors.red;
  static const grey = Colors.grey;

  static const blue = Colors.blue;
  static const yellow = Colors.yellow;
  static const amber = Colors.amber;
  static const green = Colors.green;
  static const white = Color(0xFFFFFFFF);
  static const black = Colors.black;
  static const deepPurple = Colors.deepPurple;
}
